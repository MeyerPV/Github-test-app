// This file will contain TypeScript types specific to the repository list feature
// For example, if SearchParams is only used here, it could be moved.

// We might re-export types from shared/api/types if they are directly used by this feature's model.
// export type { SearchParams } from '../../../shared/api/types'; // Example 