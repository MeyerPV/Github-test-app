// Placeholder for User entity TypeScript types

export interface User {
  id: string;
  login: string;
  avatarUrl?: string;
  name?: string;
  bio?: string;
  // Add other user-specific fields as needed
}

// Placeholder for User-related API response types if they differ from the entity
// export interface UserApiResponse { ... } 